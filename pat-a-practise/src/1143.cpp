#include <cstdio>
#include <vector>
#include <map>
#include <algorithm>
using namespace std;
int main() {
	int m, n;
	scanf("%d %d", &m, &n);
	vector<int> v(n);
	map<int, bool> m_;
	for (int i = 0; i < n; i++) {
		scanf("%d", &v[i]);
		
	}
	int query;
	for (int i = 0; i < m; i++) {
		scanf("%d", &query);

	}
}